[project]
name = "ros2-motion-safety-supervisor"
version = "0.1.0"
description = "A safety, prediction, and replay layer for ROS2 mobile robot command pipelines."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
